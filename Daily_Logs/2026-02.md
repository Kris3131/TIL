## 2026-02-17

### Frontend_Masters

**zod**
用途：確保 .env 的資料沒寫錯，並把字串轉成正確的型別（如數字、陣列）。
優點：程式啟動時就檢查，設定預設值、限制範圍，出錯立刻噴錯。

```typescript
import { env as loadEnv } from 'custom-env'
import { z } from 'zod'

process.env.APP_STAGE = process.env.APP_STAGE || 'dev'

const isProduction = process.env.APP_STAGE === 'production'
const isDevelopment = process.env.APP_STAGE === 'dev'
const isTesting = process.env.APP_STAGE === 'test'

if (isDevelopment) {
  loadEnv()
} else if (isTesting) {
  loadEnv('test')
}

const envSchema = z.object({
  NODE_ENV: z
    .enum(['development', 'test', 'production'])
    .default('development'),

  APP_STAGE: z.enum(['dev', 'test', 'production']).default('dev'),

  PORT: z.coerce.number().positive().default(3000),
  DATABASE_URL: z.string().startsWith('postgresql://'),
  JWT_SECRET: z.string().min(32, 'Must be 32 chars long'),
  JWT_EXPIRES_IN: z.string().default('7d'),
  BCRYPT_ROUNDS: z.coerce.number().min(10).max(20).default(12),
})

export type Env = z.infer<typeof envSchema>
let env: Env

try {
  env = envSchema.parse(process.env)
} catch (e) {
  if (e instanceof z.ZodError) {
    console.log('Invalid env var')
    console.error(JSON.stringify(e.flatten().fieldErrors, null, 2))

    e.issues.forEach((err) => {
      const path = err.path.join('.')
      console.log(`${path}: ${err.message}`)
    })

    process.exit(1)
  }

  throw e
}

export const isProd = () => env.APP_STAGE === 'production'
export const isDev = () => env.APP_STAGE === 'dev'
export const isTest = () => env.APP_STAGE === 'test'

export { env }
export default env

```

** express.js **
`Route Organization Strategies`
1. Resource-Based

```text
src/routes/
├── authRoutes.ts      # Authentication endpoints
├── userRoutes.ts      # User CRUD operations
├── habitRoutes.ts     # Habit CRUD operations
├── tagRoutes.ts       # Tag CRUD operations
└── index.ts           # Route aggregation (optional)
```

2. Feature-Based

```text
src/routes/
├── onboarding/
│   ├── registration.ts
│   ├── verification.ts
│   └── tutorial.ts
├── dashboard/
│   ├── overview.ts
│   ├── analytics.ts
│   └── settings.ts
└── social/
    ├── friends.ts
    ├── sharing.ts
    └── leaderboard.ts
``` 

3. Version-Based

```text
src/routes/
├── v1/
│   ├── users.ts
│   ├── habits.ts
│   └── index.ts
├── v2/
│   ├── users.ts       # Updated user endpoints
│   ├── habits.ts      # Updated habit endpoints
│   ├── analytics.ts   # New in v2
│   └── index.ts
└── index.ts           # Version routing
```

** express.js **

`middleware -> next()`

```typescript
const middleware = (req, res, next) => {
  // Do something with request/response
  next() // Pass control to next middleware
}
```

| Parameter | Type           | Purpose                                                          |
|----------|----------------|------------------------------------------------------------------|
| req      | Request object | Contains request data (body, headers, params, etc.)             |
| res      | Response object| Used to send response back to client                            |
| next     | Function       | Passes control to the next middleware in the stack              |


```typescript
const authenticate = (req, res, next) => {
  if (!req.headers.authorization) {
    res.status(401).json({ error: 'No token' })
    // ❌ No next() - request ends here!
    return
  }
  next() // ✅ Token exists, continue
}
```

If you don't call next() and don't send a response, the request will hang forever!
```typescript
// ❌ BAD - Request hangs
const badMiddleware = (req, res, next) => {
  console.log('Did something')
  // Neither next() nor res.send() called!
}

// ✅ GOOD - Either continue or respond
const goodMiddleware = (req, res, next) => {
  console.log('Did something')
  next() // Or res.send() if ending request
}
```

error handling -> next(error)

```typescript
// Pass errors to error handling middleware
const riskyMiddleware = async (req, res, next) => {
  try {
    await someDatabaseOperation()
    next() // Success - continue
  } catch (error) {
    next(error) // Error - pass to error handler
  }
}

// Error handling middleware (4 parameters!)
const errorHandler = (err, req, res, next) => {
  console.error(err.stack)
  res.status(500).json({ error: 'Something went wrong!' })
}
```